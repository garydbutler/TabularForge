<UserControl x:Class="TabularForge.UI.Views.PropertiesPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource PanelBackground}">

    <DockPanel>
        <!-- Header -->
        <Border DockPanel.Dock="Top" Padding="8,6"
                Background="{DynamicResource SideBarBackground}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <TextBlock Text="{Binding PropertiesHeader}" FontWeight="SemiBold"
                       Foreground="{DynamicResource PrimaryText}" FontSize="12" />
        </Border>

        <!-- Selected object info -->
        <Border DockPanel.Dock="Top" Padding="8,4"
                Background="{DynamicResource SideBarBackground}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1"
                Visibility="{Binding SelectedNode, Converter={StaticResource BoolToVis},
                    FallbackValue=Collapsed}">
            <StackPanel>
                <TextBlock Text="{Binding SelectedNode.Name}" FontWeight="Bold"
                           Foreground="{DynamicResource AccentBrush}" FontSize="13" />
                <TextBlock Text="{Binding SelectedNode.ObjectType}" FontSize="10"
                           Foreground="{DynamicResource SecondaryText}" Margin="0,2,0,0" />
            </StackPanel>
        </Border>

        <!-- Properties Grid -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding SelectedProperties}">
                <ItemsControl.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource SideBarBackground}"
                                        Padding="8,4" Margin="0,2,0,0"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="0,0,0,1">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                                               Foreground="{DynamicResource AccentBrush}"
                                               FontSize="11" />
                                </Border>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ItemsControl.GroupStyle>

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="8,4" BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="0,0,0,0.5">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background"
                                                    Value="{DynamicResource HoverBrush}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- Property Name -->
                                <TextBlock Grid.Column="0" Text="{Binding Name}"
                                           Foreground="{DynamicResource SecondaryText}"
                                           FontSize="11.5" VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding Description}" />

                                <!-- Property Value -->
                                <TextBlock Grid.Column="1" Text="{Binding Value}"
                                           Foreground="{DynamicResource PrimaryText}"
                                           FontSize="11.5" VerticalAlignment="Center"
                                           TextWrapping="Wrap" Margin="4,0,0,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground"
                                                    Value="{DynamicResource PrimaryText}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsReadOnly}" Value="True">
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource SecondaryText}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
