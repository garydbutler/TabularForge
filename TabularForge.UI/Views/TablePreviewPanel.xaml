<UserControl x:Class="TabularForge.UI.Views.TablePreviewPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource EditorBackground}"
             Foreground="{DynamicResource PrimaryText}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="{DynamicResource ToolBarBackground}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1"
                Padding="4,2">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Table:" VerticalAlignment="Center" Margin="4,0,6,0" FontSize="12" />
                <ComboBox ItemsSource="{Binding AvailableTables}"
                          SelectedItem="{Binding TableName}"
                          Width="200" Padding="4,3" FontSize="12" />
                <Border Width="1" Background="{DynamicResource SeparatorBrush}" Margin="6,2" />
                <TextBlock Text="Rows:" VerticalAlignment="Center" Margin="4,0,4,0" FontSize="12" />
                <TextBox Text="{Binding PreviewRowCount, UpdateSourceTrigger=PropertyChanged}"
                         Width="60" Padding="4,3" FontSize="12" />
                <Button Content="Refresh" Command="{Binding LoadPreviewCommand}"
                        Padding="8,4" Margin="6,0,2,0" FontSize="12" />
                <Border Width="1" Background="{DynamicResource SeparatorBrush}" Margin="6,2" />
                <Button Content="Export CSV" Command="{Binding ExportToCsvCommand}"
                        Padding="8,4" Margin="2,0" FontSize="12" />
            </StackPanel>
        </Border>

        <!-- Data Grid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding PreviewData}"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"
                  CanUserSortColumns="True"
                  CanUserResizeColumns="True"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  Background="{DynamicResource EditorBackground}"
                  Foreground="{DynamicResource PrimaryText}"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  FontSize="12"
                  AlternatingRowBackground="{DynamicResource AlternateRowBackground}">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Padding" Value="6,4" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>

        <!-- Loading/Error overlay -->
        <Grid Grid.Row="1" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <Border Background="#80000000">
                <TextBlock Text="Loading data..." HorizontalAlignment="Center"
                           VerticalAlignment="Center" FontSize="16" Foreground="White" />
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{DynamicResource StatusBarBackground}"
                Padding="6,3">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding StatusText}" Foreground="White" FontSize="11" Margin="0,0,16,0" />
                <TextBlock Text="{Binding RowCountText}" Foreground="White" FontSize="11" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
