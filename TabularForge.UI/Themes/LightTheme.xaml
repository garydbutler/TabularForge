<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== LIGHT THEME (Clean, professional) ===== -->

    <!-- Core Background Colors -->
    <SolidColorBrush x:Key="WindowBackground" Color="#F5F5F5" />
    <SolidColorBrush x:Key="PanelBackground" Color="#FFFFFF" />
    <SolidColorBrush x:Key="SideBarBackground" Color="#F5F5F5" />
    <SolidColorBrush x:Key="EditorBackground" Color="#FFFFFF" />
    <SolidColorBrush x:Key="TabBarBackground" Color="#ECECEC" />
    <SolidColorBrush x:Key="StatusBarBackground" Color="#0077B6" />
    <SolidColorBrush x:Key="ToolBarBackground" Color="#F0F0F0" />
    <SolidColorBrush x:Key="MenuBarBackground" Color="#E8E8E8" />

    <!-- Text Colors -->
    <SolidColorBrush x:Key="PrimaryText" Color="#2D2D2D" />
    <SolidColorBrush x:Key="SecondaryText" Color="#6E6E6E" />
    <SolidColorBrush x:Key="ActiveText" Color="#000000" />
    <SolidColorBrush x:Key="DisabledText" Color="#A0A0A0" />
    <SolidColorBrush x:Key="ErrorText" Color="#D32F2F" />
    <SolidColorBrush x:Key="WarningText" Color="#BF8803" />
    <SolidColorBrush x:Key="InfoText" Color="#0077B6" />

    <!-- Border Colors -->
    <SolidColorBrush x:Key="BorderBrush" Color="#D0D0D0" />
    <SolidColorBrush x:Key="FocusBorderBrush" Color="#0077B6" />
    <SolidColorBrush x:Key="SeparatorBrush" Color="#D0D0D0" />

    <!-- Selection / Highlight -->
    <SolidColorBrush x:Key="SelectionBrush" Color="#0077B6" />
    <SolidColorBrush x:Key="HoverBrush" Color="#0077B610" />
    <SolidColorBrush x:Key="ActiveTabBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="InactiveTabBrush" Color="#ECECEC" />
    <SolidColorBrush x:Key="HighlightBrush" Color="#C8E6F5" />
    <SolidColorBrush x:Key="CurrentLineBrush" Color="#F0F4F8" />

    <!-- Accent Colors -->
    <SolidColorBrush x:Key="AccentBrush" Color="#0077B6" />
    <SolidColorBrush x:Key="AccentHoverBrush" Color="#0096D6" />
    <SolidColorBrush x:Key="SuccessBrush" Color="#2E7D32" />
    <SolidColorBrush x:Key="WarningBrush" Color="#BF8803" />
    <SolidColorBrush x:Key="ErrorBrush" Color="#D32F2F" />

    <!-- TreeView Icon Colors -->
    <SolidColorBrush x:Key="ModelIconColor" Color="#B8860B" />
    <SolidColorBrush x:Key="TableIconColor" Color="#00897B" />
    <SolidColorBrush x:Key="ColumnIconColor" Color="#1565C0" />
    <SolidColorBrush x:Key="CalculatedColumnIconColor" Color="#7B1FA2" />
    <SolidColorBrush x:Key="MeasureIconColor" Color="#B8860B" />
    <SolidColorBrush x:Key="HierarchyIconColor" Color="#CD853F" />
    <SolidColorBrush x:Key="RelationshipIconColor" Color="#4169E1" />
    <SolidColorBrush x:Key="FolderIconColor" Color="#DAA520" />
    <SolidColorBrush x:Key="PartitionIconColor" Color="#2E7D32" />
    <SolidColorBrush x:Key="RoleIconColor" Color="#D32F2F" />
    <SolidColorBrush x:Key="PerspectiveIconColor" Color="#2E7D32" />

    <!-- Scrollbar -->
    <SolidColorBrush x:Key="ScrollBarBackground" Color="#F5F5F5" />
    <SolidColorBrush x:Key="ScrollBarThumb" Color="#C1C1C1" />
    <SolidColorBrush x:Key="ScrollBarThumbHover" Color="#A0A0A0" />

    <!-- Data Grid Alternate Row -->
    <SolidColorBrush x:Key="AlternateRowBackground" Color="#F8F9FA" />

    <!-- Input Controls -->
    <SolidColorBrush x:Key="InputBackground" Color="#FFFFFF" />
    <SolidColorBrush x:Key="InputBorderBrush" Color="#D0D0D0" />
    <SolidColorBrush x:Key="InputFocusBorderBrush" Color="#0077B6" />
    <SolidColorBrush x:Key="InputText" Color="#2D2D2D" />
    <SolidColorBrush x:Key="InputPlaceholderText" Color="#A0A0A0" />

    <!-- Button -->
    <SolidColorBrush x:Key="ButtonBackground" Color="#0077B6" />
    <SolidColorBrush x:Key="ButtonHoverBackground" Color="#0096D6" />
    <SolidColorBrush x:Key="ButtonText" Color="#FFFFFF" />
    <SolidColorBrush x:Key="ButtonBorderBrush" Color="Transparent" />

    <!-- Context Menu / Popup -->
    <SolidColorBrush x:Key="PopupBackground" Color="#FFFFFF" />
    <SolidColorBrush x:Key="PopupBorderBrush" Color="#D0D0D0" />
    <SolidColorBrush x:Key="MenuItemHoverBrush" Color="#0077B620" />

    <!-- ===== Global Control Styles ===== -->

    <Style TargetType="Window" x:Key="DarkWindow">
        <Setter Property="Background" Value="{StaticResource WindowBackground}" />
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
    </Style>

    <Style TargetType="TreeView">
        <Setter Property="Background" Value="{StaticResource SideBarBackground}" />
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <Style TargetType="TreeViewItem">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
        <Setter Property="Padding" Value="2,1" />
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource HighlightBrush}" />
                <Setter Property="Foreground" Value="{StaticResource ActiveText}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource HoverBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource InputBackground}" />
        <Setter Property="Foreground" Value="{StaticResource InputText}" />
        <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}" />
        <Setter Property="CaretBrush" Value="{StaticResource PrimaryText}" />
        <Setter Property="Padding" Value="4,3" />
        <Setter Property="BorderThickness" Value="1" />
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource InputFocusBorderBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Menu">
        <Setter Property="Background" Value="{StaticResource MenuBarBackground}" />
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
        <Setter Property="Background" Value="Transparent" />
    </Style>

    <Style TargetType="ToolBar">
        <Setter Property="Background" Value="{StaticResource ToolBarBackground}" />
    </Style>

    <Style TargetType="StatusBar">
        <Setter Property="Background" Value="{StaticResource StatusBarBackground}" />
        <Setter Property="Foreground" Value="{StaticResource ActiveText}" />
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="6,3" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="3">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="#0077B615" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="#0077B630" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource DisabledText}" />
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{StaticResource PanelBackground}" />
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
    </Style>

    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource EditorBackground}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
    </Style>

    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource PanelBackground}" />
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
        <Setter Property="RowBackground" Value="{StaticResource PanelBackground}" />
        <Setter Property="AlternatingRowBackground" Value="{StaticResource SideBarBackground}" />
        <Setter Property="GridLinesVisibility" Value="Horizontal" />
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderBrush}" />
    </Style>

    <Style TargetType="Expander">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
        <Setter Property="Background" Value="{StaticResource PanelBackground}" />
    </Style>

    <Style TargetType="GroupBox">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
        <Setter Property="Background" Value="{StaticResource PanelBackground}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
    </Style>

    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{StaticResource PopupBackground}" />
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
        <Setter Property="BorderBrush" Value="{StaticResource PopupBorderBrush}" />
    </Style>

    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource PopupBackground}" />
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
        <Setter Property="BorderBrush" Value="{StaticResource PopupBorderBrush}" />
    </Style>

    <Style TargetType="ScrollViewer">
        <Setter Property="Background" Value="Transparent" />
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource InputBackground}" />
        <Setter Property="Foreground" Value="{StaticResource InputText}" />
        <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}" />
    </Style>

</ResourceDictionary>
